# MaxGamer AI角色知识库管理

本目录用于管理MaxGamer平台中所有AI角色的知识库文件。每个角色都有独立的子目录，用于定义其性格、对话风格、职责和行为准则。

---

## 📁 目录结构

```
knowledge_base/
├── README.md                 # 本文件，知识库管理说明
├── max/                      # Max角色目录
│   └── character.md          # Max角色知识库
├── [其他角色]/               # 未来添加的其他角色目录
│   └── character.md          # 角色知识库文件
└── ...
```

### 目录组织原则
- 每个角色拥有独立的子目录
- 目录名使用角色名称（小写）
- 主要知识库文件统一命名为 `character.md`
- 可在角色目录下添加其他相关文件（如对话示例、配置文件等）

---

## 🎭 当前角色列表

### 1. Max - 登录页AI助手
- **目录**: [`max/`](max/)
- **文件**: [`max/character.md`](max/character.md)
- **位置**: 登录页面（`login.html`）
- **职责**: 欢迎用户、介绍平台、回应互动
- **状态**: ✅ 已部署
- **平台**: MaxGamer

---

## 📝 知识库文件规范

每个角色的知识库文件应包含以下部分：

### 1. 角色基本信息
- 角色名称
- 角色定位
- 性格特点
- 语言风格

### 2. 角色背景故事
- 角色的来历和使命
- 与平台的关系
- 核心价值观

### 3. 核心职责
- 主要任务和目标
- 服务场景
- 交互方式

### 4. 对话风格指南
- 语言特点
- Emoji使用规范
- 禁止内容

### 5. 典型对话场景
- 各种触发场景
- 示例台词
- 回复要点

### 6. 平台知识库
- 相关平台信息
- 功能介绍
- 价值观

### 7. AI行为准则
- 角色保持
- 用户尊重
- 内容边界

---

## 🔧 如何使用知识库

### 在AI API中使用

知识库内容应作为**系统提示词（System Prompt）**的一部分传递给AI模型：

```python
# 示例：在 ai_dialogue.py 中使用
from pathlib import Path

def load_character_knowledge(character_name):
    """加载角色知识库"""
    file_path = Path(__file__).parent / 'knowledge_base' / character_name / 'character.md'
    with open(file_path, 'r', encoding='utf-8') as f:
        return f.read()

# 构建系统提示词
character_knowledge = load_character_knowledge('max')
system_prompt = f"""
{character_knowledge}

请根据以上角色设定回复用户。
"""
```

### 在前端中引用

前端可以通过API获取角色的预设消息：

```javascript
// 示例：在 ai-dialogue.js 中
const characterConfig = {
    name: 'Max',
    directory: 'max',
    knowledgeBase: '/api/characters/max/knowledge',
    fallbackMessages: {
        intro: [...],
        like: [...],
        gift: [...],
        comment: [...]
    }
};
```

---

## ➕ 添加新角色

### 步骤1: 创建角色目录

在本目录下创建新的角色子目录：

```bash
mkdir knowledge_base/[角色名]
```

### 步骤2: 创建知识库文件

在角色目录下创建 `character.md` 文件：

```bash
touch knowledge_base/[角色名]/character.md
```

### 步骤3: 填写角色信息

参考 [`max/character.md`](max/character.md) 的结构，填写完整的角色信息。

### 步骤4: 更新本README

在"当前角色列表"部分添加新角色的信息。

### 步骤5: 集成到代码

- 在后端API中添加新角色的路由
- 在前端创建对应的对话模块
- 测试并验证功能

---

## 🔄 知识库更新流程

### 1. 定期审查
- 每月审查一次所有角色知识库
- 根据用户反馈调整对话风格
- 更新平台信息和功能介绍

### 2. 版本控制
- 每次更新在文件末尾记录更新日志
- 使用语义化版本号（v1.0.0）
- 重大更新需要团队评审

### 3. 测试验证
- 更新后进行对话测试
- 确保符合角色设定
- 验证用户体验

---

## 📊 知识库质量标准

### 内容完整性
- ✅ 包含所有必需部分
- ✅ 示例台词丰富多样
- ✅ 场景覆盖全面

### 语言质量
- ✅ 表达清晰准确
- ✅ 风格统一一致
- ✅ 符合角色定位

### 实用性
- ✅ 易于AI理解和执行
- ✅ 便于开发者维护
- ✅ 支持快速迭代

---

## 🛠️ 维护工具

### 知识库验证脚本

```python
# validate_knowledge_base.py
from pathlib import Path

def validate_character_directory(character_dir):
    """验证角色目录的完整性"""
    character_file = character_dir / 'character.md'
    
    if not character_file.exists():
        return False, "缺少 character.md 文件"
    
    required_sections = [
        '角色基本信息',
        '角色背景故事',
        '核心职责',
        '对话风格指南',
        '典型对话场景',
        '平台知识库',
        'AI行为准则'
    ]
    
    with open(character_file, 'r', encoding='utf-8') as f:
        content = f.read()
        for section in required_sections:
            if section not in content:
                return False, f"缺少必需部分: {section}"
    
    return True, "验证通过"
```

### 对话测试工具

```python
# test_character_dialogue.py
def test_character_responses(character_name, scenarios):
    """测试角色在各种场景下的回复"""
    # 测试逻辑...
```

---

## 📚 参考资源

### AI对话设计最佳实践
- [Conversational AI Design Guide](https://example.com)
- [Character Development for Chatbots](https://example.com)
- [System Prompt Engineering](https://example.com)

### 相关文档
- [XMGamer平台架构文档](../ARCHITECTURE.md)
- [AI API使用指南](../routes/ai_dialogue.py)
- [前端对话模块文档](../../frontend/js/ai-dialogue.js)
- [Max角色知识库](max/character.md)

---

## 👥 贡献指南

### 如何贡献

1. Fork本项目
2. 创建特性分支 (`git checkout -b feature/new-character`)
3. 提交更改 (`git commit -m 'Add new character'`)
4. 推送到分支 (`git push origin feature/new-character`)
5. 创建Pull Request

### 贡献规范

- 遵循现有的目录结构和文件格式
- 提供清晰的更新说明
- 包含必要的测试和验证
- 更新相关文档

---

## 📞 联系方式

如有问题或建议，请联系：

- **项目负责人**: XMGamer开发团队
- **邮箱**: dev@xmgamer.com
- **文档更新**: 2024-12-01

---

## 📄 许可证

本知识库内容归XMGamer平台所有，仅供内部使用。