# FrameWorker 一键部署说明

## 🚀 首次部署（完整安装）

首次部署时运行此脚本，会安装所有依赖：

```cmd
deploy-final.bat
```

## ⚡ 快速更新（仅更新代码）

后续更新代码时使用此脚本，只需几秒钟：

```cmd
update-code.bat
```

## 📋 脚本功能

该脚本会自动完成以下操作：

1. ✅ 自动下载 PuTTY 工具（plink/pscp）用于自动输入密码
2. ✅ 创建服务器目录结构
3. ✅ 上传后端代码（Python）
4. ✅ 上传前端代码（HTML/CSS/JS）
5. ✅ 上传部署脚本
6. ✅ 在服务器上自动执行部署
7. ✅ 配置 Nginx 和 systemd 服务
8. ✅ 启动应用

**全程无需手动输入密码！**

## 🔧 服务器信息

- **IP**: 149.88.69.87
- **用户**: root
- **密码**: pXw1995（已内置在脚本中）
- **项目目录**: /var/www/xmframer

## 📝 部署后访问

部署完成后，可以通过以下地址访问：

- **前端**: http://149.88.69.87
- **API健康检查**: http://149.88.69.87/api/health

## 🛠️ 常用管理命令

### 查看服务状态
```powershell
# 如果安装了 plink
plink -batch -pw pXw1995 root@149.88.69.87 "sudo systemctl status frameworker"

# 或使用标准 SSH
ssh root@149.88.69.87 "sudo systemctl status frameworker"
```

### 查看实时日志
```powershell
plink -batch -pw pXw1995 root@149.88.69.87 "sudo journalctl -u frameworker -f"
```

### 重启服务
```powershell
plink -batch -pw pXw1995 root@149.88.69.87 "sudo systemctl restart frameworker"
```

### 编辑配置文件
```powershell
plink -batch -pw pXw1995 root@149.88.69.87 "nano /var/www/xmframer/backend/.env"
```

## ⚙️ 配置 API 密钥

部署完成后，需要配置 AI 图像生成的 API 密钥：

1. 登录服务器：
```powershell
ssh root@149.88.69.87
```

2. 编辑配置文件：
```bash
nano /var/www/xmframer/backend/.env
```

3. 添加你的 API 密钥：
```env
AI_IMAGE_API_KEY=your_api_key_here
```

4. 重启服务：
```bash
sudo systemctl restart frameworker
```

## 🔄 更新代码

如果需要更新代码，只需再次运行部署脚本：

```powershell
powershell -ExecutionPolicy Bypass -File one-click-deploy.ps1
```

脚本会自动覆盖旧文件并重启服务。

## ❓ 故障排查

### 如果部署失败

1. **检查网络连接**：
```powershell
ping 149.88.69.87
```

2. **检查 SSH 连接**：
```powershell
ssh root@149.88.69.87
```

3. **查看服务日志**：
```bash
sudo journalctl -u frameworker -n 100
```

4. **检查 Nginx 状态**：
```bash
sudo systemctl status nginx
sudo nginx -t
```

### 如果服务无法启动

1. 检查 Python 依赖是否安装：
```bash
cd /var/www/xmframer/backend
source venv/bin/activate
pip list
```

2. 检查端口占用：
```bash
sudo netstat -tlnp | grep 3000
```

3. 手动启动测试：
```bash
cd /var/www/xmframer/backend
source venv/bin/activate
python app.py
```

## 📚 相关文档

- [快速部署指南.md](快速部署指南.md) - 详细的手动部署步骤
- [DEPLOY_PYTHON.md](DEPLOY_PYTHON.md) - Python 后端部署说明
- [README_DEPLOY.md](README_DEPLOY.md) - 完整部署文档

## 🎯 技术栈

- **后端**: Python 3.x + Flask
- **前端**: HTML5 + CSS3 + JavaScript
- **Web服务器**: Nginx
- **进程管理**: systemd
- **操作系统**: Ubuntu 24.04

## 💡 提示

- 脚本会自动下载 PuTTY 工具到临时目录
- 所有密码都已内置，无需手动输入
- 部署过程大约需要 3-5 分钟
- 首次部署会安装所有依赖，后续更新会更快

---

**祝部署顺利！** 🎉