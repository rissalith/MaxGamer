# 版本管理指南

## ❌ 版本号不会自动更改

**重要**：Git和GitHub **不会自动更改版本号**，你需要手动管理版本。

---

## 📋 版本更新完整流程

### 场景：你修改了代码，想发布新版本

#### 1️⃣ 本地开发和测试
```bash
# 修改代码...
# 本地测试通过
```

#### 2️⃣ 提交代码到Git
```bash
git add .
git commit -m "feat: 添加新功能的描述"
```

#### 3️⃣ 手动打版本标签
```bash
# 根据改动类型选择版本号
git tag -a v1.1.0 -m "版本说明"
```

#### 4️⃣ 推送到GitHub
```bash
git push origin master
git push origin --tags
```

#### 5️⃣ 在GitHub创建Release（可选）
访问：https://github.com/rissalith/FrameWorker/releases/new

---

## 🔢 版本号规则（语义化版本）

格式：`v主版本.次版本.修订号`

### 什么时候改哪个数字？

#### 修订号（v1.0.X）- 小改动
**改动类型**：
- 修复Bug
- 小的优化
- 文档更新
- 不影响功能的改进

**示例**：
```bash
# 修复了一个生成失败的Bug
git tag -a v1.0.1 -m "fix: 修复生成失败的问题"

# 优化了性能
git tag -a v1.0.2 -m "perf: 优化图片处理性能"
```

#### 次版本号（v1.X.0）- 新功能
**改动类型**：
- 添加新功能
- 功能增强
- 向后兼容的改动

**示例**：
```bash
# 添加了新的AI模型支持
git tag -a v1.1.0 -m "feat: 支持DALL-E模型"

# 添加了批量生成功能
git tag -a v1.2.0 -m "feat: 添加批量生成功能"
```

#### 主版本号（vX.0.0）- 重大更新
**改动类型**：
- 重大架构调整
- 不兼容的API变更
- 完全重写

**示例**：
```bash
# 重构了整个后端
git tag -a v2.0.0 -m "refactor: 重构后端架构"

# 改用新的AI模型，不兼容旧版
git tag -a v2.0.0 -m "breaking: 迁移到新AI模型"
```

---

## 📝 实际操作示例

### 示例1：修复Bug（v1.0.0 → v1.0.1）

```bash
# 1. 修改代码修复Bug
# 编辑 backend/app.py

# 2. 测试修复
cd backend && start.bat

# 3. 提交
git add .
git commit -m "fix: 修复生成时的内存泄漏问题"

# 4. 打标签（修订号+1）
git tag -a v1.0.1 -m "修复生成时的内存泄漏问题"

# 5. 推送
git push origin master
git push origin --tags

# 6. 部署到服务器
deploy-final.bat
```

### 示例2：添加新功能（v1.0.1 → v1.1.0）

```bash
# 1. 开发新功能
# 添加了视频导出功能

# 2. 测试功能
# 本地测试通过

# 3. 提交
git add .
git commit -m "feat: 添加视频导出功能"

# 4. 打标签（次版本号+1，修订号归0）
git tag -a v1.1.0 -m "添加视频导出功能"

# 5. 推送
git push origin master
git push origin --tags

# 6. 部署
deploy-final.bat
```

### 示例3：重大更新（v1.1.0 → v2.0.0）

```bash
# 1. 重构代码
# 完全重写了AI生成逻辑

# 2. 测试
# 全面测试

# 3. 提交
git add .
git commit -m "refactor: 重构AI生成引擎"

# 4. 打标签（主版本号+1，其他归0）
git tag -a v2.0.0 -m "重构AI生成引擎，不兼容v1.x"

# 5. 推送
git push origin master
git push origin --tags

# 6. 部署
deploy-final.bat
```

---

## 🚀 快速版本发布脚本

我为你创建一个快速发布脚本，简化版本管理：

### release.bat（Windows）
```batch
@echo off
echo ================================
echo 快速版本发布脚本
echo ================================
echo.

REM 检查是否有未提交的更改
git status --short
if errorlevel 1 (
    echo 错误：Git仓库有问题
    pause
    exit /b 1
)

echo.
echo 请选择版本类型：
echo 1. 修订版本 (v1.0.X) - Bug修复
echo 2. 次版本 (v1.X.0) - 新功能
echo 3. 主版本 (vX.0.0) - 重大更新
echo.
set /p choice=请输入选择 (1/2/3): 

REM 获取当前版本
for /f "tokens=*" %%i in ('git describe --tags --abbrev=0 2^>nul') do set current_version=%%i
if "%current_version%"=="" set current_version=v1.0.0

echo 当前版本: %current_version%
echo.

set /p new_version=请输入新版本号 (例如 v1.0.1): 
set /p message=请输入版本说明: 

echo.
echo 准备发布版本: %new_version%
echo 版本说明: %message%
echo.
set /p confirm=确认发布? (y/n): 

if /i not "%confirm%"=="y" (
    echo 已取消
    pause
    exit /b 0
)

REM 提交代码
git add .
git commit -m "%message%"

REM 打标签
git tag -a %new_version% -m "%message%"

REM 推送
git push origin master
git push origin --tags

echo.
echo ================================
echo 版本发布成功！
echo ================================
echo 版本: %new_version%
echo GitHub: https://github.com/rissalith/FrameWorker/releases
echo.
echo 下一步：
echo 1. 访问 GitHub 创建 Release
echo 2. 运行 deploy-final.bat 部署到服务器
echo.
pause
```

---

## 📊 版本历史示例

```
v1.0.0 - 2025-11-22 - 初始版本
v1.0.1 - 2025-11-23 - 修复生成失败问题
v1.0.2 - 2025-11-24 - 优化性能
v1.1.0 - 2025-11-25 - 添加视频导出功能
v1.1.1 - 2025-11-26 - 修复视频导出Bug
v1.2.0 - 2025-11-27 - 添加批量生成功能
v2.0.0 - 2025-12-01 - 重构AI引擎
```

---

## 🔍 查看版本信息

### 查看所有版本
```bash
git tag -l
```

### 查看版本详情
```bash
git show v1.0.0
```

### 查看当前版本
```bash
git describe --tags
```

### 查看版本之间的差异
```bash
git diff v1.0.0 v1.1.0
```

---

## ⚠️ 常见错误

### 错误1：忘记推送标签
```bash
# 只推送了代码，忘记推送标签
git push origin master

# 解决：补推标签
git push origin --tags
```

### 错误2：版本号打错了
```bash
# 删除本地标签
git tag -d v1.0.1

# 删除远程标签
git push origin :refs/tags/v1.0.1

# 重新打标签
git tag -a v1.0.1 -m "正确的说明"
git push origin v1.0.1
```

### 错误3：版本号跳跃
```bash
# 错误：从 v1.0.0 直接跳到 v1.2.0
# 正确：应该是 v1.0.0 → v1.1.0 → v1.2.0
```

---

## 📚 最佳实践

1. **每次发布前先测试** - 确保功能正常
2. **写清楚版本说明** - 方便以后查看
3. **遵循语义化版本** - 让版本号有意义
4. **小步快跑** - 频繁发布小版本
5. **保持版本连续** - 不要跳版本号
6. **同步服务器** - 发布后记得部署

---

## 🎯 总结

**版本号需要手动管理，不会自动更改！**

每次更新流程：
```
修改代码 → 测试 → git commit → git tag → git push → 部署服务器
```

记住：
- **小改动** → 修订号 +1 (v1.0.X)
- **新功能** → 次版本 +1 (v1.X.0)
- **大更新** → 主版本 +1 (vX.0.0)

---

**需要帮助？** 查看 [`开发部署工作流.md`](开发部署工作流.md)